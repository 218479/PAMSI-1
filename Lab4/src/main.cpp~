#include <iostream>
#include "element.h"
#include "kolejka_head.h"
#include "Test_tablica.hh"

using namespace std;

int main()
{
    kolejka_head Nowa;
time_t start, stop;
double trwanie;
    Nowa.push(1000000000);
    Test_tablica Test;
    Test.set_tablice(Nowa.pop());
start=clock();
    Test.merge_sort(Test.get_tab(),0,999999999);
stop=clock();
trwanie=(double)(start-stop)/CLOCKS_PER_SEC;
    return 0;
}
